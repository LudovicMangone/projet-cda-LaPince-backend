# 🗓️ Suivi quotidien — Sprint 3 - Day 3

## 📅 Date

11/06/2026

---

## 🚀 Activités personnelles du jour

### Frontend

* Modification de l’illustration de la page 403.
* Création d’une page dédiée pour les erreurs 500.
* Mise en place d’une redirection vers la page 500 lors des erreurs serveur rencontrées sur le chargement d’un projet.
* Mise en place d’une gestion centralisée des erreurs API dans `services/api.ts` via les fonctions `handleResponse` et `handleDeleteResponse`.

  * Gestion des erreurs 401.
  * Gestion des erreurs 429 avec affichage d’un toast global.
  * Gestion des erreurs 500 avec affichage d’un toast global.
  * Gestion homogène des erreurs réseau.
* Création d’un dossier dédié aux validations frontend.
* Séparation volontaire entre :

  * les validations UI légères côté frontend ;
  * les validations métier et de sécurité côté backend avec Zod.
* Mise en place des premiers retours utilisateurs directement sous les champs des formulaires lors des erreurs de saisie.

### Backend

#### Couverture de tests

* Recherche sur le comportement du coverage affiché à 100 % sur les routes alors que les contrôleurs, services et middlewares n’étaient pas réellement comptabilisés.
* Amélioration de la configuration de couverture.
* Analyse du fonctionnement de V8 et identification de la cause :

  * les requêtes HTTP passent par la couche réseau ;
  * les middlewares, contrôleurs et services s’exécutent correctement ;
  * cependant V8 ne les comptabilise pas comme couverts dans cette architecture de tests d’intégration, ce qui explique les statistiques à 0 % sur certaines couches applicatives.

Conclusion : à travailler plus tard, le coverage est un bonus !

#### Tests

Ajout de tests unitaires et d’intégration pour :

* `validateProjectUpdate`
* `validateProjectParticipantsUpdate`
* `projects.controller.ts`
* `GET /api/projects/:id`
* `PATCH /api/projects/:id`
* `PATCH /api/projects/:id/participants`
* `DELETE /api/projects/:id`

#### Correctifs

* Harmonisation de la gestion des erreurs Zod dans les middlewares.

  * Choix de laisser remonter les `ZodError` vers l’`errorHandler` (422) plutôt que de les transformer en `BadRequestError` (400).
  * Alignement avec le comportement déjà utilisé dans `createProjectController`.
* Correction de plusieurs incohérences de codes de statut HTTP dans la gestion des erreurs.

---

## 🎯 Prochaines étapes

* Finaliser les retours utilisateurs sur les formulaires.
* Étendre les validations frontend aux autres formulaires de l’application.
* Continuer l’amélioration de la gestion centralisée des erreurs.
* Poursuivre l’augmentation de la couverture de tests backend.
* Faire la version black de l'image pour la page erreur 500.
